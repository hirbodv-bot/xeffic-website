(function(){
  const pageKey = document.body && document.body.dataset.cmsPage;
  if(!pageKey) return;
  const norm=s=>(s||'').replace(/\s+/g,' ').trim();
  const setText=(el,value)=>{if(!el||value===undefined||value===null||value==='')return; if(norm(el.textContent)!==norm(value))el.textContent=value;};
  const createImage=(src,alt,cls)=>{const i=document.createElement('img');i.src=src||'';i.alt=alt||'';i.className=cls||'';i.loading='lazy';return i;};
  async function getJSON(url){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error(url+' '+r.status);return r.json();}
  function applyTheme(t){if(!t)return;const r=document.documentElement, map={brand_color:'--brand',brand_secondary:'--brand2',accent_color:'--accent',background_color:'--bg',surface_color:'--surface',text_color:'--ink',muted_text_color:'--muted',dark_color:'--dark'};Object.entries(map).forEach(([k,v])=>{if(t[k])r.style.setProperty(v,t[k]);});}
  function applyNavigation(site){if(!site.navigation_override||!Array.isArray(site.navigation)||!site.navigation.length)return;document.querySelectorAll('.main-nav,.nav-links').forEach(nav=>{nav.innerHTML='';site.navigation.forEach(x=>{if(!x||!x.label||!x.url)return;const a=document.createElement('a');a.textContent=x.label;a.href=x.url;if(x.is_cta)a.className='nav-cta';nav.appendChild(a);});});}
  function applyFooterLinks(site){if(!site.footer_links_override||!Array.isArray(site.footer_columns)||!site.footer_columns.length)return;document.querySelectorAll('footer .footer-grid').forEach(grid=>{const first=grid.firstElementChild;[...grid.children].slice(1).forEach(x=>x.remove());site.footer_columns.forEach(col=>{const d=document.createElement('div');const h=document.createElement('h3');h.textContent=col.title||'';d.appendChild(h);(col.links||[]).forEach(x=>{if(!x.label||!x.url)return;const a=document.createElement('a');a.textContent=x.label;a.href=x.url;d.appendChild(a);});grid.appendChild(d);});});}
  function applySite(site){
    applyTheme(site.theme); applyNavigation(site); applyFooterLinks(site);
    if(site.email){document.querySelectorAll('a[href^="mailto:"]').forEach((a,i)=>{if(i===0||!site.secondary_email){a.href='mailto:'+site.email;if(norm(a.textContent).includes('@'))a.textContent=site.email;}});}
    if(site.secondary_email){const mails=document.querySelectorAll('a[href^="mailto:"]');if(mails[1]){mails[1].href='mailto:'+site.secondary_email;if(norm(mails[1].textContent).includes('@'))mails[1].textContent=site.secondary_email;}}
    if(site.linkedin_url){document.querySelectorAll('a').forEach(a=>{if(norm(a.textContent).toLowerCase()==='linkedin')a.href=site.linkedin_url;});}
    if(site.footer_text){document.querySelectorAll('footer .footer-grid > div:first-child p').forEach(p=>setText(p,site.footer_text));}
    if(site.logo_light){document.querySelectorAll('.site-header .brand-logo img,.site-header .logo-img').forEach(img=>img.src=site.logo_light);}
    if(site.logo_dark){document.querySelectorAll('.site-footer .brand-logo img,footer .footer-logo').forEach(img=>img.src=site.logo_dark);}
    if(site.favicon){let f=document.querySelector('link[rel="icon"]');if(!f){f=document.createElement('link');f.rel='icon';document.head.appendChild(f);}f.href=site.favicon;}
    if(site.footer_bottom_left){const x=document.querySelector('footer .footer-bottom span:first-child');if(x)setText(x,site.footer_bottom_left);}
    if(site.footer_bottom_right){const x=document.querySelector('footer .footer-bottom span:last-child');if(x)setText(x,site.footer_bottom_right);}
  }
  function applyButtons(sec,data){const btns=[...sec.querySelectorAll('a.btn')];if(btns[0]){setText(btns[0],data.button_1_text);if(data.button_1_url)btns[0].href=data.button_1_url;}if(btns[1]){setText(btns[1],data.button_2_text);if(data.button_2_url)btns[1].href=data.button_2_url;}}
  function applyCard(article,card){if(!article||!card)return;if(card.hide_card){article.style.display='none';return;}article.style.display='';const kicker=article.querySelector('.kicker,.tag,.eyebrow');setText(kicker,card.kicker);setText(article.querySelector('h2,h3,h4'),card.title);setText(article.querySelector('p'),card.text);const a=article.querySelector('a[href]');if(a){setText(a,card.link_label);if(card.link_url)a.href=card.link_url;}if(card.background_image){article.style.backgroundImage='url("'+card.background_image.replace(/"/g,'')+'")';article.style.backgroundSize='cover';article.style.backgroundPosition=card.image_position||'center';}if(Array.isArray(card.bullets)&&card.bullets.length){const ul=article.querySelector('ul');if(ul){const lis=[...ul.querySelectorAll(':scope > li')];card.bullets.forEach((t,i)=>{if(lis[i])setText(lis[i],t);else{const li=document.createElement('li');li.textContent=t;ul.appendChild(li);}});lis.slice(card.bullets.length).forEach(li=>li.remove());}}if(card.image){let img=article.querySelector(':scope > img.cms-card-image');if(!img){img=createImage(card.image,card.image_alt,'cms-card-image');article.insertBefore(img,article.firstChild);}else{img.src=card.image;img.alt=card.image_alt||'';}img.style.objectPosition=card.image_position||'center';}}
  function applyItem(el,item){if(!el||!item)return;if(item.hide_item){el.style.display='none';return;}el.style.display='';const strong=el.querySelector('strong,b');setText(strong,item.title);const span=el.querySelector('span');if(span)setText(span,item.text);else if(item.text&&strong)el.appendChild(document.createTextNode(' '+item.text));if(item.image){let img=el.querySelector(':scope > img.cms-item-image');if(!img){img=createImage(item.image,item.image_alt,'cms-item-image');el.appendChild(img);}else{img.src=item.image;img.alt=item.image_alt||'';}img.style.objectPosition=item.image_position||'center';}}
  function applySection(sec,data){if(!sec||!data)return;if(data.hide_section){sec.style.display='none';return;}sec.style.display='';if(data.background_image){sec.style.backgroundImage='url("'+data.background_image.replace(/"/g,'')+'")';sec.style.backgroundSize='cover';sec.style.backgroundPosition='center';}
    const sh=sec.querySelector('.section-head');let eyebrow,heading,intro;if(sh){eyebrow=sh.querySelector('.eyebrow,.kicker');heading=sh.querySelector('h1,h2');intro=sh.querySelector('p');}else{const box=sec.querySelector(':scope > .container')||sec;eyebrow=box.querySelector(':scope > .eyebrow,:scope > .kicker,.pill');heading=box.querySelector(':scope > h1,:scope > h2');if(!heading){const h=box.querySelector('h1,h2');if(h&&!h.closest('article'))heading=h;}intro=[...box.querySelectorAll('p')].find(p=>!p.closest('article')&&!p.closest('form'))||null;}setText(eyebrow,data.eyebrow);setText(heading,data.title);setText(intro,data.text);applyButtons(sec,data);
    if(data.image){const existing=sec.querySelector('[data-cms-primary-image="true"]');if(existing){existing.src=data.image;existing.alt=data.image_alt||existing.alt||'';existing.style.objectPosition=data.image_position||'center';}else{let wrap=sec.querySelector(':scope .cms-section-image-wrap');if(!wrap){wrap=document.createElement('div');wrap.className='container cms-section-image-wrap';wrap.appendChild(createImage(data.image,data.image_alt,'cms-section-image'));sec.appendChild(wrap);}const img=wrap.querySelector('img');img.src=data.image;img.alt=data.image_alt||'';img.style.objectPosition=data.image_position||'center';}}
    if(Array.isArray(data.gallery)&&data.gallery.length){let g=sec.querySelector('.cms-section-gallery');if(!g){g=document.createElement('div');g.className='container cms-section-gallery';sec.appendChild(g);}g.innerHTML='';data.gallery.forEach(src=>g.appendChild(createImage(src,'','cms-gallery-image')));}
    (data.cards||[]).forEach(c=>{if(c.id)applyCard(sec.querySelector('[data-cms-card="'+CSS.escape(c.id)+'"]'),c);});(data.items||[]).forEach(i=>{if(i.id)applyItem(sec.querySelector('[data-cms-item="'+CSS.escape(i.id)+'"]'),i);});
  }
  Promise.allSettled([getJSON('/content/site.json'),getJSON('/content/'+pageKey+'.json')]).then(rs=>{const site=rs[0].status==='fulfilled'?rs[0].value:null,page=rs[1].status==='fulfilled'?rs[1].value:null;if(site)applySite(site);if(page){if(page.seo){if(page.seo.title)document.title=page.seo.title;const d=document.querySelector('meta[name="description"]');if(d&&page.seo.description)d.content=page.seo.description;if(page.seo.og_image){let og=document.querySelector('meta[property="og:image"]');if(!og){og=document.createElement('meta');og.setAttribute('property','og:image');document.head.appendChild(og);}og.content=page.seo.og_image;}}(page.sections||[]).forEach(data=>{if(data.id)applySection(document.querySelector('[data-cms-section="'+CSS.escape(data.id)+'"]'),data);});}});
})();